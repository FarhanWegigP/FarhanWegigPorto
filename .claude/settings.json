{
  "permissions": {
    "allow": [
      "WebFetch(domain:doi.org)",
      "WebSearch",
      "Bash(tasklist)",
      "Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3000)",
      "Bash(npm run *)"
    ]
  }
}
